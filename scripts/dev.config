#!/bin/bash

REPO_BRANCH=$2

DEPLOY_SERVER=(192.9.215.2)
DEPLOY_PATH=/var/vhosts
DEPLOY_USER=root
APP_NAME=projectn
GIT_CHECKOUT="git checkout -b $REPO_BRANCH remotes/origin/$REPO_BRANCH &&"

echo $GIT_CHECKOUT
